import { extendTheme } from "native-base";

const THEME = extendTheme({
  colors: {
    primary: {
      100: "#000000",
    },
    secondary: {
      100: "#ffffff",
    },
  },
});

export default THEME;
